<table class="table is-fullwidth">
  <tbody>
    <%= render partial: 'index_tbody', layout: 'index_tr', collection: @oauth_users, as: :model %>
    <%= render partial: 'provider_tbody', layout: 'provider_tr', collection: current_user.valid_providers, as: :model %>
  </tbody>
</table>
