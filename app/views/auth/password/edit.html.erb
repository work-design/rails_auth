<div class="six wide column">
  <h2 class="ui header">Reset your password!</h2>

  <%= form_with model: User.new, scope: '', url: password_reset_url(params[:token]), class: 'ui attached form segment'  do |f| %>
    <%= f.password_field :password, css: { wrap_input: 'ten wide column' } %>
    <%= f.password_field :password_confirmation, css: { wrap_input: 'ten wide column' } %>
    <%= f.submit 'confirm' %>
  <% end %>
  
</div>

