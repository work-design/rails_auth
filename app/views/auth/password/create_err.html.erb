<div class="four wide column">
  <h2 class="ui block top attached centered header"><%= t('.title') %></h2>
  <div class="ui bottom attached segment">
    <%= link_to t('.sign'), sign_url(identity: @identity) %>
  </div>
</div>
