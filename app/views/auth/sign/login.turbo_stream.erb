<%= turbo_stream.append 'body' do %>
  <div
    data-controller="visit"
    data-visit-method-value="<%= session.delete(:request_method) %>"
    data-visit-params-value="<%= session.delete(:request_body).to_json %>"
    data-visit-url-value="<%= url %>"
  >
  </div>
<% end %>
