$('#modal').modal('hide')
<% if params[:form_id] %>
var form = document.getElementById('<%= params[:form_id] %>')
if (form) {
  form.submit()
}
<% else %>
Turbolinks.visit('<%= session[:return_to] %>')
<% end %>
