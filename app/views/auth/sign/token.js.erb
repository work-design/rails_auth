<% if @verify_token.user %>
document.getElementById('password').parentNode.innerHTML = '<%= j(text_field_tag :token, nil, placeholder: User.human_attribute_name(:token), class: 'field input') %>'
<% end %>
