<% if @verify_token.user && @verify_token.user.password_digest.present? %>
document.getElementById('password').parentNode.innerHTML = '<%= j(text_field_tag :token, nil, placeholder: VerifyToken.human_attribute_name(:token), class: 'field input') %>'
<% end %>
