<header class="ui borderless menu">
  <div class="ui container">

    <% if current_user %>
      <%= link_to login_path, class: 'item header' do %>
        <span>登录</span>
      <% end %>
    <% else %>
      <%= link_to '首页', root_path, class: 'item header' %>
    <% end %>

    <div class="right menu">
      <% if current_user %>
        <div class="ui simple dropdown item" id="navbar_dropdown">
          <%= image_tag current_user.avatar, class: 'ui avatar image' if current_user.avatar_attachment %>
          <%= current_user.name %>
          <i class="dropdown icon"></i>
          <div class="menu">
            <%= link_to 'Profile', '/my/profile', class: active_helper(controllers: ['profile']) %>
            <%= link_to 'Admin', '/admin', class: 'item' %>
            <%= link_to '退出', logout_path, data: { confirm: '确认退出' }, class: 'item' %>
          </div>
        </div>
      <% else %>
        <%= link_to login_path, class: 'item' do %>
          <i class="icon-user"></i> 登录/注册
        <% end %>
      <% end %>
    </div>

  </div>
</header>


