<%= render 'filter_form' %>

<%= render layout: 'index_table' do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @user_tags, as: :model %>
<% end %>

<%= paginate @user_tags %>
