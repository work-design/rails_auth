<%= form_with model: @user_tagged, url: { action: 'create', user_tag_id: @user_tag.id } do |f| %>
  <%= render 'shared/error_messages', target: @user_tagged %>
  <%= f.select :user_id, options_for_select(UserTagged.options_i18n(:tagged_type)) %>
  <%= f.submit %>
<% end %>
