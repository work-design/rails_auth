<%= form_with model: @user, url: { action: 'update_user_tags' } do |f| %>
  <%= render 'error_messages', target: f.object %>
  <%= f.collection_check_boxes :user_tag_ids, @user_tags, :id, :name %>
  <%= f.submit %>
<% end %>
