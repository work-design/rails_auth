<td>
  <p><%= model.type_i18n %></p>
  <p><%= model.appid %></p>
</td>
<td>
  <p class="has-text-success"><%= model.uid %></p>
  <p class="has-text-grey"><%= model.unionid %></p>
</td>
<td>
  <div class="is-several">
    <%= image_tag model.avatar_url, class: 'image is-24x24' if model.avatar_url.present? %>
    <span><%= model.name %></span>
  </div>
</td>
<td><time data-controller="time"><%= model.created_at.to_s(:rfc822) %></time></td>
<td>
  <%= link_to model.user.id, { controller: 'users', action: 'show', id: model.user.id }, data: { turbo_frame: 'modal' }, text: true if model.user %>
</td>
