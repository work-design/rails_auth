<table class="ui very basic large table">
  <thead>
    <tr>
      <th class="six wide">
        <%= link_to t('.edit'), edit_panel_authorized_token_path(@authorized_token), class: 'ui mini blue right floated button', remote: true %>
      </th>
      <th class="ten wide"></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td class="right aligned"><%= AuthorizedToken.human_attribute_name(:token) %></td>
      <td><%= @authorized_token.token %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= AuthorizedToken.human_attribute_name(:expire_at) %></td>
      <td><%= @authorized_token.expire_at %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= AuthorizedToken.human_attribute_name(:session_key) %></td>
      <td><%= @authorized_token.session_key %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= AuthorizedToken.human_attribute_name(:oauth_user) %></td>
      <td><%= @authorized_token.oauth_user %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= AuthorizedToken.human_attribute_name(:account) %></td>
      <td><%= @authorized_token.account %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= AuthorizedToken.human_attribute_name(:user) %></td>
      <td><%= @authorized_token.user %></td>
    </tr>
  </tbody>
</table>
