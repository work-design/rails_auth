<td>
  <p><%= model.identity %></p>
  <p class="has-text-grey"><%= model.uid %></p>
  <p><%= model.token %></p>
  <p><time data-controller="time" class="<%= model.expired? ? 'has-text-danger' : 'has-text-success' %>"><%= model.expire_at.to_s(:rfc822) %></time></p>
  <p><%= model.session_key %></p>
</td>
