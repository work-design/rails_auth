<div class="three wide column">
  <h4 class="ui block top attached centered header">请注册</h4>
  <div class="ui bottom attached segment">
    <%= form_with scope: nil, url: join_path, local: @local, on: { label: nil, wrapper_input: false, placeholder: true }, css: { wrapper_all: 'field', submit: 'ui fluid primary button' } do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>
</div>
