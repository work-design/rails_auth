<%= form_with model: @user, url: join_path, local: true, on: { wrapper_input: false } do |f| %>
  <%= render 'shared/error_messages', target: @user %>
  <%= f.email_field :email, placeholder: 'Email', label: false %>
  <%= f.password_field :password, placeholder: 'Password', label: false %>
  <%= f.password_field :password_confirmation, placeholder: 'Password confirm', label: false %>
  <%= f.submit 'submit' %>
<% end %>
