<%= javascript_tag nonce: true do %>
  Turbo.visit('<%= url_for({ controller: '/auth/sign', action: 'sign', form_id: params[:form_id], identity: params[:identity] }) %>', { action: 'replace' })
<% end %>
